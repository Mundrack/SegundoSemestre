//punteros: es una variable en si, se usa para el paso por referencia, para que devuelva los valores que quiera y no solo un valor en una funcion.
// como inicializar: float *puntero;
//*puntero=0; esto da error.
// float aux=30;
// *puntero=30;   ahi no da problema.
//primero contenido 
//este es para asignacion: puntero=&aux (es la direccion de memoria)
//para cambiar los datos seria *puntero=40; pero tambien alteraria al aux porque tienen la misma direccion de memoria
//* con asterisco cambio el contenido
// sin asterisco asigno ubicacion de memoria.

